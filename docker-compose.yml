version: "3.8"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: phishingapp
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: changeme
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data:
